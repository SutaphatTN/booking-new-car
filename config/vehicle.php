<?php

return [
    /*
    |--------------------------------------------------------------------------
    | วันที่เริ่มใช้งานเมนูป้ายทะเบียน (go-live)
    |--------------------------------------------------------------------------
    | รถที่ "ส่งมอบ" (DeliveryDate) ก่อนวันนี้จะถูกซ่อนออกจากหน้ารายการป้ายทะเบียน
    | เพื่อไม่ให้ข้อมูลเก่าก่อนเปิดใช้เมนูมารกหน้าจอ — ข้อมูลยังอยู่ใน DB ครบ
    |
    | รูปแบบ: 'YYYY-MM-DD' (เช่น '2026-06-01')
    | เว้นว่าง / null = ไม่กรอง แสดงทั้งหมดเหมือนเดิม
    */
    'registration_start_date' => env('VEHICLE_REGISTRATION_START_DATE', null),

    /*
    |--------------------------------------------------------------------------
    | รายการ accessory_price ที่เป็น "มัดจำป้ายแดง"
    |--------------------------------------------------------------------------
    | id ของ accessory_price ที่ถูกเลือกจากหน้าการจองในรูปแบบประดับยนต์
    | เพื่อใช้เป็นค่ามัดจำป้ายแดง (รุ่นละ 1 รายการ) — ใช้แสดงในหน้าป้ายแดง
    | ว่าการจองนี้มีการเก็บมัดจำป้ายและใช้ยอดเท่าไร
    */
    'plate_deposit_accessory_ids' => [329, 336, 345, 354, 362, 368, 375, 382],
];
